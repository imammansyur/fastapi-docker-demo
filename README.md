# fastapi-docker-demo
This project demonstrates containerizing a simple FastAPI application using Docker and Docker Compose

The base FastAPI example is adapted from the GeeksforGeeks FastAPI tutorial.

## Running FastAPI as container
To run the FastAPI app using Docker, run this command:

```
docker compose up
```

Or if you're using Podman, run this instead:

```
podman compose up
```
